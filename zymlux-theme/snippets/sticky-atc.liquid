{%- comment -%}
  Disabled — this legacy gold sticky add-to-cart bar (French "AJOUTER")
  has been replaced by the Zymlux sticky bar built into the product
  template (#zx-pbar, English). Kept as an empty snippet so any
  remaining {% render 'sticky-atc' %} calls output nothing.
{%- endcomment -%}
