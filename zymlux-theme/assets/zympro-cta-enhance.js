/* Disabled — legacy ZymPro CTA script (built the gold French "Ajouter"
   sticky bar, injected "Ajouté · Voir mon panier" feedback and French
   price labels). Replaced by the Zymlux product template's own sticky
   bar and button logic. Kept as a no-op so the script tag in
   sections/main-product.liquid resolves without a 404. */
